// This hook is no longer needed as we're using real database data
// Keeping file for backward compatibility but hook does nothing
export function useDataInitialization() {
  // No-op - Dashboard now uses real database queries
}